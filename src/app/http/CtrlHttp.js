/**
 * [CtrlHttp description]
 * this is controller for feature 'http'
 */
define( [ 'app' ], function ( app ) {
  app.registerController( 'CtrlHttp', [ '$scope', function ( $scope ) {
    // controller code here
  }]);
});
/**
 * [description]
 * this is a spec for CtrlDemo
 * use command-line 'grunt test' to run specs
 */
define( [ 'angular', 'angular-mock', './CtrlDemo' ], function ( angular ) {
  describe( 'Test for CtrlDemo', function () {
    beforeEach( function () {
      module( 'app' ); //mock app
    });
    var Ctrl, scope;
    // Initialize the controller and a mock scope
    beforeEach( inject( function ( $rootScope, $controller ) {
        scope = $rootScope.$new();
        Ctrl = $controller( 'CtrlDemo', { $scope:scope } );
    }));
    // spec code here
    it('should attach a list of awesomeThings to the scope', function () {
        expect(true).toBeTruthy();
    });

  });
});